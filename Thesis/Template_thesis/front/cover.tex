\begin{abstractpage}
This is a sample abstract. Replace this paragraph with a concise description of the research problem, methods, main results, and conclusions. The abstract page uses the same font and line-spacing settings as the thesis body.

The template supports mathematical expressions, such as
\[
  \min_{x\in\mathbb{R}^d} F(x) := f(x)+g(x),
\]
but the abstract should normally remain brief and readable.

\keywords{keyword one, keyword two, keyword three}
\end{abstractpage}
