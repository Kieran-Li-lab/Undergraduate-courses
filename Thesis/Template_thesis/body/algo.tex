\chapter{Methods}

\section{Algorithm Table}

Algorithm \ref{alg:sample} demonstrates the thesis-style algorithm table. The algorithm counter is independent from the table counter.
\begin{table}[htbp]
\vspace{0.5em}\centering\wuhao
\refstepcounter{algorithm}
\label{alg:sample}
\begin{tabularx}{\linewidth}{@{}lY@{}}
\toprule
\textbf{Algorithm \thealgorithm} & \textbf{Sample algorithm} \\
\midrule
\textbf{Input}
& Initial point $x_0$, stepsize $\gamma$, and maximum iteration number $K$.\\
\vspace{0.2em}
\textbf{Step 1}
& For $k=0,1,\ldots,K-1$, repeat the following computations:\par
\hspace*{2em}1) Compute the gradient $\nabla f(x_k)$;\par
\hspace*{2em}2) Update
$x_{k+1}=x_k-\gamma\nabla f(x_k)$;\par
\hspace*{2em}3) Check the stopping criterion.\\
\textbf{Output}
& $x_K$.\\
\bottomrule
\end{tabularx}
\end{table}

\section{Theorem-like Environments}

\begin{definition}[Convex function]\label{def:convex}
A function $f:\mathbb{R}^{d}\to(-\infty,+\infty]$ is convex if, for any $x,y\in\operatorname{dom}f$ and any $t\in[0,1]$,
\begin{equation}\label{eq:convex}
  f(tx+(1-t)y)\le t f(x)+(1-t)f(y).
\end{equation}
\end{definition}

\begin{lemma}\label{lem:sample}
If $f$ is convex, then every local minimizer of $f$ is also a global minimizer.
\end{lemma}

\begin{proof}
This follows from the definition of convexity.
\end{proof}

\begin{theorem}\label{thm:sample}
Under suitable assumptions, the sequence generated by Algorithm \ref{alg:sample} is well defined.
\end{theorem}

\begin{proof}
Replace this sample proof with the actual proof.
\end{proof}

\begin{remark}
The theorem, lemma, definition, assumption, proposition, corollary, example, and remark environments are numbered independently by chapter.
\end{remark}
